# Spec consolidation

This feature supersedes and absorbs scope from:

| Feature | Status | Notes |
|---------|--------|-------|
| `shadcn-ui-migration` | **Done** (committed) | React + shadcn + assistant-ui Thread landed in `78e2f00` |
| `streaming-intent` | **Partial** | Inline status deferred; reasoning/text split fixed in CC-GUI-002 |
| `pi-tauri-migration` | **Done** | Pi RPC + Tauri CC is production path; Kirigami replaced |
| `kirigami-settings` | **Frozen** | QML app; stabilization track separate from CC |
| `settings-stabilization` | **Frozen** | agntos-settings only; not CC |

Active work for Control Centre v2 lives only in `cc-gui-v2/`.
