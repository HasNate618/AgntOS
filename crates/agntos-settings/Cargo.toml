[package]
name = "agntos-settings"
version = "0.1.0"
edition = "2021"

[dependencies]
agnt-common = { path = "../agnt-common" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
